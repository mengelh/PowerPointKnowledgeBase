http:
  routers:
    authentik:
      rule: "PathPrefix(`/authentik`)"
      entryPoints:
        - websecure
      service: authentik
      tls: {}

  services:
    authentik:
      loadBalancer:
        servers:
          - url: "http://authentik-server:9000"
