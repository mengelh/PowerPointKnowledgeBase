http:
  routers:
    traefik-dashboard:
      rule: "PathPrefix(`/traefik`)"
      entryPoints:
        - websecure
      service: api@internal
      middlewares:
        - traefik-strip-prefix@file
        # authentik-forward-auth@file — re-enable after Authentik ForwardAuth provider is configured

  middlewares:
    traefik-strip-prefix:
      stripPrefix:
        prefixes:
          - "/traefik"
