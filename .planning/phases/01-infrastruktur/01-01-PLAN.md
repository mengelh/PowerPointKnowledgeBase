---
phase: 01-infrastruktur
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ansible/requirements.yml
  - ansible/inventory/hosts.yml
  - ansible/inventory/group_vars/all.yml
  - ansible/playbooks/deploy-infrastructure.yml
  - ansible/roles/docker/tasks/main.yml
  - ansible/roles/traefik/tasks/main.yml
  - ansible/roles/traefik/handlers/main.yml
  - ansible/roles/traefik/vars/main.yml
  - ansible/roles/traefik/templates/docker-compose.yml.j2
  - ansible/roles/traefik/templates/traefik.yml.j2
  - ansible/roles/traefik/templates/middlewares.yml.j2
  - ansible/roles/traefik/templates/dashboard.yml.j2
  - ansible/roles/traefik/templates/tls.yml.j2
autonomous: true
requirements:
  - TRFK-01
  - TRFK-02
  - TRFK-03
  - TRFK-04
  - TRFK-05
  - TRFK-06
  - DEPL-01
  - DEPL-02

must_haves:
  truths:
    - "HTTPS terminates at Traefik; curl https://[DOMAIN] returns a response (self-signed cert accepted with -k)"
    - "HTTP requests redirect to HTTPS — curl -v http://[DOMAIN] returns 301 pointing to https://"
    - "The proxy Docker network exists and Traefik is attached to it"
    - "Traefik static config loads from traefik.yml (not CLI flags) with exposedByDefault: false"
    - "Access logs are written in JSON format to the Traefik container log stream"
    - "Prometheus metrics endpoint responds at http://traefik:8082/metrics (internal port)"
    - "The Traefik dashboard router is defined in dynamic file config and references the authentik-forward-auth middleware"
  artifacts:
    - path: "ansible/roles/docker/tasks/main.yml"
      provides: "Docker Engine + Compose plugin installation on target VM"
    - path: "ansible/roles/traefik/templates/docker-compose.yml.j2"
      provides: "Traefik Compose definition with proxy network, volumes, port bindings"
    - path: "ansible/roles/traefik/templates/traefik.yml.j2"
      provides: "Static config: entrypoints, providers, accessLog JSON, prometheus, api"
    - path: "ansible/roles/traefik/templates/middlewares.yml.j2"
      provides: "Dynamic: authentik-forward-auth middleware with full authResponseHeaders list"
    - path: "ansible/roles/traefik/templates/dashboard.yml.j2"
      provides: "Dynamic: traefik-dashboard router pointing to api@internal"
    - path: "ansible/roles/traefik/templates/tls.yml.j2"
      provides: "Dynamic: TLS options (self-signed, minVersion TLS12)"
  key_links:
    - from: "ansible/roles/traefik/templates/middlewares.yml.j2"
      to: "ansible/roles/traefik/templates/dashboard.yml.j2"
      via: "middleware reference name authentik-forward-auth"
      pattern: "authentik-forward-auth"
    - from: "ansible/roles/traefik/tasks/main.yml"
      to: "community.docker.docker_network"
      via: "proxy network creation before docker_compose_v2"
      pattern: "docker_network"
    - from: "ansible/roles/traefik/templates/traefik.yml.j2"
      to: "ansible/roles/traefik/templates/dashboard.yml.j2"
      via: "file provider watches /config/dynamic directory"
      pattern: "directory.*dynamic"
---

<objective>
Create the Ansible docker role and Traefik role with all Jinja2 templates, static/dynamic config, and the top-level playbook structure. When this plan is complete, running the Ansible playbook against the test VM will install Docker, create the proxy network, deploy Traefik with HTTPS (self-signed), HTTP-to-HTTPS redirect, JSON access logs, Prometheus metrics, and a ForwardAuth-ready dashboard router. The Authentik containers do not yet exist, so the dashboard will return a 500 on the forwardAuth check — that is expected and fixed in Plan 02.

Purpose: Establish the proxy network and Traefik as the TLS-terminating entry point that all future services join. Nothing can be routed or authenticated until this layer exists.

Output: Ansible project skeleton + docker role + Traefik role with Jinja2 templates; playbook runs to completion on the test VM; Traefik container is up with self-signed TLS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Key decisions locked by user discussion. Executor must not deviate. -->

TLS: Self-signed certificate (NOT Let's Encrypt / ACME). Use Traefik's built-in self-signed: no
certificatesResolvers block; routers use `tls: true` with no certresolver. Traefik auto-generates
a self-signed cert when tls is enabled without a resolver.

Domain: home-engelhardt.uk (exact apex domain for AUTHENTIK_COOKIE_DOMAIN in Plan 02)

Architecture:
  - traefik/docker-compose.yml — Traefik only
  - authentik/docker-compose.yml — Authentik stack (Plan 02)
  - External Docker network named "proxy" (created by Traefik role, referenced externally by Authentik role)

Traefik static config: mounted file at /config/traefik.yml (NOT CLI command: flags)
Traefik dynamic config: /config/dynamic/ directory, file provider with watch: true

ForwardAuth middleware: defined in dynamic/middlewares.yml (file provider), NOT Docker labels.
Reference in routers via "authentik-forward-auth@file" suffix.

Dashboard: dynamic/dashboard.yml — router rule PathPrefix(`/traefik`), service api@internal,
middleware authentik-forward-auth@file. Dashboard protected from Plan 01 day one.

Self-signed TLS: dynamic/tls.yml — TLS options with minVersion: VersionTLS12.
Routers use `tls: true` (no certresolver).

Prometheus metrics: entrypoint "metrics" on port 8082 (internal only, not published to host).
accessLog format: json

Ansible module: community.docker.docker_compose_v2 (NOT docker_compose).
Never use: docker compose down -v. Use: docker compose up -d --remove-orphans.
Pull strategy: missing (pull only if image not present — prevents churn, supports pinned tags).

Secrets: group_vars/all/vault.yml (encrypted) — NOT plain vars. Plan 01 only uses non-secret vars.
Non-secret vars go in group_vars/all.yml.

deploy_base_dir: /opt/kb-pipeline
traefik_image: traefik:v3.3
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ansible project skeleton — docker role and top-level playbook</name>
  <files>
    ansible/requirements.yml
    ansible/inventory/hosts.yml
    ansible/inventory/group_vars/all.yml
    ansible/playbooks/deploy-infrastructure.yml
    ansible/roles/docker/tasks/main.yml
  </files>

  <read_first>
    - .planning/research/STACK.md (Ansible Playbook Pattern section, requirements.yml format, package task)
    - .planning/research/ARCHITECTURE.md (Ansible Role / Directory Structure section, Variable handling table)
  </read_first>

  <action>
Create the following files exactly as specified. All paths relative to repo root.

**ansible/requirements.yml**
```yaml
---
collections:
  - name: community.docker
    version: ">=3.12.0"
```

**ansible/inventory/hosts.yml**
```yaml
---
all:
  hosts:
    kb-vm:
      ansible_host: "{{ lookup('env', 'KB_VM_HOST') | default('kb-vm.home-engelhardt.uk') }}"
      ansible_user: deploy
      ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
```
Note: ansible_host uses an env var override so Semaphore can inject the real IP without editing the file.

**ansible/inventory/group_vars/all.yml**
```yaml
---
# Non-secret shared variables — safe to commit plain text
domain: home-engelhardt.uk
deploy_base_dir: /opt/kb-pipeline
traefik_image: traefik:v3.3
traefik_log_level: INFO
```

**ansible/playbooks/deploy-infrastructure.yml**
```yaml
---
- name: Deploy KB-Pipeline Infrastructure (Traefik + Authentik)
  hosts: all
  become: true
  roles:
    - role: docker
    - role: traefik
    - role: authentik
```

**ansible/roles/docker/tasks/main.yml**
```yaml
---
- name: Install Docker prerequisites
  ansible.builtin.package:
    name:
      - ca-certificates
      - curl
      - gnupg
    state: present

- name: Add Docker GPG key
  ansible.builtin.shell:
    cmd: |
      install -m 0755 -d /etc/apt/keyrings
      curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
        gpg --dearmor -o /etc/apt/keyrings/docker.gpg
      chmod a+r /etc/apt/keyrings/docker.gpg
    creates: /etc/apt/keyrings/docker.gpg

- name: Add Docker apt repository
  ansible.builtin.shell:
    cmd: |
      echo \
        "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
        https://download.docker.com/linux/ubuntu \
        $(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
        tee /etc/apt/sources.list.d/docker.list > /dev/null
    creates: /etc/apt/sources.list.d/docker.list

- name: Install Docker Engine and Compose plugin
  ansible.builtin.apt:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-compose-plugin
      - python3-docker
    state: present
    update_cache: true

- name: Ensure Docker service is enabled and started
  ansible.builtin.systemd:
    name: docker
    enabled: true
    state: started

- name: Add deploy user to docker group
  ansible.builtin.user:
    name: "{{ ansible_user }}"
    groups: docker
    append: true
```

The `creates:` guards on the shell tasks make them idempotent — they skip if the output file already exists.
  </action>

  <verify>
    <automated>
      cd /home/dev/Projects/PowerPointKnowledgeBase && \
      test -f ansible/requirements.yml && \
      test -f ansible/inventory/hosts.yml && \
      test -f ansible/inventory/group_vars/all.yml && \
      test -f ansible/playbooks/deploy-infrastructure.yml && \
      test -f ansible/roles/docker/tasks/main.yml && \
      grep -q "community.docker" ansible/requirements.yml && \
      grep -q "domain: home-engelhardt.uk" ansible/inventory/group_vars/all.yml && \
      grep -q "docker_compose_plugin\|docker-compose-plugin" ansible/roles/docker/tasks/main.yml && \
      echo "PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - ansible/requirements.yml declares community.docker >= 3.12.0
    - ansible/inventory/group_vars/all.yml contains domain: home-engelhardt.uk and deploy_base_dir: /opt/kb-pipeline
    - ansible/roles/docker/tasks/main.yml installs docker-ce, docker-compose-plugin, python3-docker
    - The docker tasks use creates: guards or package module (idempotent by default)
    - No secret values (passwords, keys) appear in any of these files
    - ansible/playbooks/deploy-infrastructure.yml lists roles: docker, traefik, authentik in that order
  </acceptance_criteria>

  <done>All five files exist with correct content. ansible-lint ansible/playbooks/deploy-infrastructure.yml passes with no errors on YAML structure.</done>
</task>

<task type="auto">
  <name>Task 2: Traefik role — tasks, handlers, vars, and all Jinja2 templates</name>
  <files>
    ansible/roles/traefik/tasks/main.yml
    ansible/roles/traefik/handlers/main.yml
    ansible/roles/traefik/vars/main.yml
    ansible/roles/traefik/templates/docker-compose.yml.j2
    ansible/roles/traefik/templates/traefik.yml.j2
    ansible/roles/traefik/templates/middlewares.yml.j2
    ansible/roles/traefik/templates/dashboard.yml.j2
    ansible/roles/traefik/templates/tls.yml.j2
  </files>

  <read_first>
    - .planning/research/ARCHITECTURE.md (Traefik Static vs Dynamic Configuration section, Network Topology section, Ansible Idempotency Patterns section)
    - .planning/research/STACK.md (Traefik v3 Docker Compose Labels section, Authentik Forward Auth Middleware section, "If the test-VM has no public internet exposure" variant section)
    - .planning/research/PITFALLS.md (Pitfall 1, Pitfall 2, Pitfall 3, Pitfall 6, Pitfall 10)
  </read_first>

  <action>
Create all files below. Read the interface block in the plan context section before writing — it contains locked decisions (self-signed TLS, file-provider middleware, proxy network naming).

**ansible/roles/traefik/vars/main.yml**
```yaml
---
traefik_config_dir: "{{ deploy_base_dir }}/traefik/config"
traefik_dynamic_dir: "{{ deploy_base_dir }}/traefik/config/dynamic"
traefik_compose_dir: "{{ deploy_base_dir }}/traefik"
```

**ansible/roles/traefik/handlers/main.yml**
```yaml
---
- name: restart traefik
  community.docker.docker_compose_v2:
    project_src: "{{ traefik_compose_dir }}"
    state: present
    recreate: auto
    remove_orphans: true
```

**ansible/roles/traefik/tasks/main.yml**
```yaml
---
- name: Create Traefik directory structure
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    owner: root
    group: root
    mode: "0755"
  loop:
    - "{{ traefik_compose_dir }}"
    - "{{ traefik_config_dir }}"
    - "{{ traefik_dynamic_dir }}"

- name: Create proxy Docker network
  community.docker.docker_network:
    name: proxy
    driver: bridge
    state: present

- name: Template Traefik static config
  ansible.builtin.template:
    src: traefik.yml.j2
    dest: "{{ traefik_config_dir }}/traefik.yml"
    owner: root
    mode: "0644"
  notify: restart traefik

- name: Template ForwardAuth middleware dynamic config
  ansible.builtin.template:
    src: middlewares.yml.j2
    dest: "{{ traefik_dynamic_dir }}/middlewares.yml"
    owner: root
    mode: "0644"
  notify: restart traefik

- name: Template dashboard router dynamic config
  ansible.builtin.template:
    src: dashboard.yml.j2
    dest: "{{ traefik_dynamic_dir }}/dashboard.yml"
    owner: root
    mode: "0644"
  notify: restart traefik

- name: Template TLS options dynamic config
  ansible.builtin.template:
    src: tls.yml.j2
    dest: "{{ traefik_dynamic_dir }}/tls.yml"
    owner: root
    mode: "0644"
  notify: restart traefik

- name: Template Traefik Docker Compose file
  ansible.builtin.template:
    src: docker-compose.yml.j2
    dest: "{{ traefik_compose_dir }}/docker-compose.yml"
    owner: root
    mode: "0644"
  notify: restart traefik

- name: Ensure Traefik stack is running
  community.docker.docker_compose_v2:
    project_src: "{{ traefik_compose_dir }}"
    state: present
    pull: missing
    remove_orphans: true
```

**ansible/roles/traefik/templates/docker-compose.yml.j2**
```yaml
services:
  traefik:
    image: {{ traefik_image }}
    container_name: traefik
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - {{ traefik_config_dir }}/traefik.yml:/etc/traefik/traefik.yml:ro
      - {{ traefik_dynamic_dir }}:/etc/traefik/dynamic:ro
    networks:
      - proxy
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    labels:
      - "traefik.enable=true"

networks:
  proxy:
    name: proxy
    driver: bridge
```

Note: The Traefik container is on the proxy network so Docker provider can resolve authentik-server
by hostname when it appears. The static config (traefik.yml) is mounted read-only; dynamic config
directory is also mounted read-only (Ansible manages files; Traefik only reads them).

**ansible/roles/traefik/templates/traefik.yml.j2**
```yaml
global:
  checkNewVersion: false
  sendAnonymousUsage: false

api:
  dashboard: true
  insecure: false

ping: {}

entryPoints:
  web:
    address: ":80"
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
          permanent: true
  websecure:
    address: ":443"
  metrics:
    address: ":8082"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false
    network: proxy
  file:
    directory: "/etc/traefik/dynamic"
    watch: true

log:
  level: {{ traefik_log_level }}

accessLog:
  format: json

metrics:
  prometheus:
    entryPoint: metrics
    addRoutersLabels: true
    addServicesLabels: true
```

Key points:
- No certificatesResolvers block (self-signed TLS, per user decision)
- exposedByDefault: false (TRFK-01)
- HTTP redirect to HTTPS on web entrypoint (TRFK-03)
- accessLog json format (TRFK-05)
- Prometheus on dedicated metrics entrypoint port 8082 (TRFK-06)
- ping: {} enables the healthcheck endpoint

**ansible/roles/traefik/templates/middlewares.yml.j2**
```yaml
http:
  middlewares:
    authentik-forward-auth:
      forwardAuth:
        address: "http://authentik-server:9000/outpost.goauthentik.io/auth/traefik"
        trustForwardHeader: true
        authResponseHeaders:
          - X-authentik-username
          - X-authentik-groups
          - X-authentik-email
          - X-authentik-name
          - X-authentik-uid
          - X-authentik-jwt
          - X-authentik-meta-jwks
          - X-authentik-meta-outpost
          - X-authentik-meta-provider
          - X-authentik-meta-app
          - X-authentik-meta-version
```

Note: address uses internal HTTP (not HTTPS) to avoid TLS redirect loop (Pitfall 2).
Full authResponseHeaders list prevents Pitfall 3 (missing headers to upstream).

**ansible/roles/traefik/templates/dashboard.yml.j2**
```yaml
http:
  routers:
    traefik-dashboard:
      rule: "PathPrefix(`/traefik`)"
      entryPoints:
        - websecure
      service: api@internal
      tls: {}
      middlewares:
        - authentik-forward-auth@file
```

Note: tls: {} with no certresolver = Traefik generates self-signed cert (per user decision TRFK-02).
middleware authentik-forward-auth@file references the file provider definition in middlewares.yml.
No ForwardAuth on this router is NOT correct — the dashboard MUST be protected. This IS protected.

**ansible/roles/traefik/templates/tls.yml.j2**
```yaml
tls:
  options:
    default:
      minVersion: VersionTLS12
      cipherSuites:
        - TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
        - TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384
        - TLS_ECDHE_RSA_WITH_CHACHA20_POLY1305_SHA256
        - TLS_AES_128_GCM_SHA256
        - TLS_AES_256_GCM_SHA384
        - TLS_CHACHA20_POLY1305_SHA256
      sniStrict: false
```
  </action>

  <verify>
    <automated>
      cd /home/dev/Projects/PowerPointKnowledgeBase && \
      test -f ansible/roles/traefik/tasks/main.yml && \
      test -f ansible/roles/traefik/handlers/main.yml && \
      test -f ansible/roles/traefik/templates/docker-compose.yml.j2 && \
      test -f ansible/roles/traefik/templates/traefik.yml.j2 && \
      test -f ansible/roles/traefik/templates/middlewares.yml.j2 && \
      test -f ansible/roles/traefik/templates/dashboard.yml.j2 && \
      test -f ansible/roles/traefik/templates/tls.yml.j2 && \
      grep -q "exposedByDefault: false" ansible/roles/traefik/templates/traefik.yml.j2 && \
      grep -q "format: json" ansible/roles/traefik/templates/traefik.yml.j2 && \
      grep -q "prometheus" ansible/roles/traefik/templates/traefik.yml.j2 && \
      grep -q "authentik-forward-auth" ansible/roles/traefik/templates/middlewares.yml.j2 && \
      grep -q "http://authentik-server:9000" ansible/roles/traefik/templates/middlewares.yml.j2 && \
      grep -q "api@internal" ansible/roles/traefik/templates/dashboard.yml.j2 && \
      grep -q "docker_network" ansible/roles/traefik/tasks/main.yml && \
      grep -q "docker_compose_v2" ansible/roles/traefik/tasks/main.yml && \
      grep -q "docker_compose_v2" ansible/roles/traefik/handlers/main.yml && \
      echo "PASS"
    </automated>
  </verify>

  <acceptance_criteria>
    - traefik.yml.j2 has exposedByDefault: false under providers.docker (TRFK-01)
    - traefik.yml.j2 has no certificatesResolvers block (self-signed TLS, not Let's Encrypt)
    - traefik.yml.j2 has accessLog: format: json (TRFK-05)
    - traefik.yml.j2 has metrics.prometheus with entryPoint: metrics (TRFK-06)
    - traefik.yml.j2 has HTTP-to-HTTPS redirect on web entrypoint (TRFK-03)
    - middlewares.yml.j2 forwardAuth address is http://authentik-server:9000/... (NOT https)
    - middlewares.yml.j2 has all 11 X-authentik-* authResponseHeaders listed
    - middlewares.yml.j2 has trustForwardHeader: true
    - dashboard.yml.j2 router references authentik-forward-auth@file (protected, TRFK-04)
    - dashboard.yml.j2 has tls: {} (self-signed, TRFK-02)
    - dashboard.yml.j2 service is api@internal
    - docker-compose.yml.j2 mounts docker.sock as :ro
    - docker-compose.yml.j2 has healthcheck using traefik healthcheck --ping
    - tasks/main.yml creates the proxy network via community.docker.docker_network before docker_compose_v2
    - tasks/main.yml uses community.docker.docker_compose_v2 (not docker_compose or shell)
    - handlers/main.yml uses community.docker.docker_compose_v2 with state: present, recreate: auto
    - No HTTPS URL appears in the forwardAuth address
  </acceptance_criteria>

  <done>All 8 files exist. grep checks in verify pass. The Traefik role can be executed by Ansible against the test VM: directories created, proxy network created, config files templated, Traefik container started with self-signed TLS.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the following checks against the repo:

```bash
# 1. Verify no plain-text secrets in any Ansible file
git grep -i "password\|secret_key\|pg_pass" ansible/ | grep -v vault | grep -v ".j2:.*vault_"
# Expected: no output (no plain-text secrets)

# 2. Verify community.docker.docker_compose_v2 is used consistently (not docker_compose)
grep -r "docker_compose:" ansible/
# Expected: no output (v1 module not used)

# 3. Verify forwardAuth uses internal HTTP URL
grep -r "https://authentik" ansible/roles/traefik/
# Expected: no output (HTTPS forwardAuth would cause redirect loop)

# 4. Verify exposedByDefault is false
grep "exposedByDefault" ansible/roles/traefik/templates/traefik.yml.j2
# Expected: exposedByDefault: false

# 5. Verify dashboard is protected (would be a blocker if missing)
grep "authentik-forward-auth" ansible/roles/traefik/templates/dashboard.yml.j2
# Expected: line with authentik-forward-auth@file
```
</verification>

<success_criteria>
- All files listed in files_modified exist in the repository
- ansible/requirements.yml declares community.docker >= 3.12.0
- Traefik role templates implement TRFK-01 through TRFK-06
- No secrets appear in plain text in any committed file
- Traefik role tasks are idempotent: community.docker.docker_network + community.docker.docker_compose_v2 with state: present
- The proxy network creation precedes the docker_compose_v2 task in tasks/main.yml
- ForwardAuth middleware uses internal HTTP URL (http://authentik-server:9000/...), not HTTPS
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastruktur/01-01-SUMMARY.md` with:
- Files created and their purpose
- Key config decisions made (self-signed TLS approach, proxy network creation order, file provider for middleware)
- Pitfalls explicitly avoided and how (list Pitfall numbers from PITFALLS.md)
- What the executor did NOT build (Authentik containers — that is Plan 02)
- Any version pins used (traefik:v3.3, etc.)
</output>
